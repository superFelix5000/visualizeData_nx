import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'io.ionic.starter',
    appName: 'ionic-dashboard',
    webDir: '../../dist/apps/ionic-dashboard',
    bundledWebRuntime: false,
};

export default config;
